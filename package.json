{
  "name": "dsstudio",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npx lerna bootstrap",
    "dev": "npx lerna run start",
    "clean": "npx lerna clean",
    "outdated": "npx lerna exec -- npm outdated",
    "update": "npx lerna exec -- npm update",
    "publish": "npx lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
