{
  "name": "dsstudio",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "npx lerna bootstrap",
    "bootstrap": "npx lerna bootstrap",
    "build": "npx lerna run build",
    "clean": "npx lerna clean && npx lerna run clean",
    "update": "npx lerna exec -- npm update",
    "publish": "npx lerna publish -- --access public"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
