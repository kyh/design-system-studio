import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { Box, Heading, Flex } from "../";
import { Text } from "./Text";

<Meta title="Components/Text" component={Text} />

# Text

Primitive typographic component, defaults to `<p>`. Use the `as` prop to set the correct HTML element independent from styling.

<Preview withToolbar>
  <Story name="text">
    <Box>
      <Flex alignItems="baseline" mb="md">
        <Text variant="hint" width="lg">
          body
        </Text>
        <Text variant="body">You know nothing, Jon Snow</Text>
      </Flex>
      <Flex alignItems="baseline" mb="md">
        <Text variant="hint" width="lg">
          caption
        </Text>
        <Text variant="caption">You know nothing, Jon Snow</Text>
      </Flex>
      <Flex alignItems="baseline" mb="md">
        <Text variant="hint" width="lg">
          hint
        </Text>
        <Text variant="hint">You know nothing, Jon Snow</Text>
      </Flex>
      <Flex alignItems="baseline" mb="md">
        <Text variant="hint" width="lg">
          label
        </Text>
        <Text variant="label">You know nothing, Jon Snow</Text>
      </Flex>
    </Box>
  </Story>
</Preview>

## Component Props

<Props of={Text} />
