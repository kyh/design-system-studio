import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import { Tabs } from "./Tabs";
import { Tab } from "./Tab";

<Meta title="Components/Tab" component={Tab} />

# Tabs

Tabs allow for an easy way to organize similar information into groups, without having to use separate pages to do so.

<Canvas withToolbar>
  <Story name="default">
    <Tabs>
      <Tab className="active">My Projects</Tab>
      <Tab>Discover</Tab>
      <Tab>GitHub</Tab>
    </Tabs>
  </Story>
</Canvas>

## Component Props

<ArgsTable of={Tab} />
