import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { Grid } from "./grid";
import { Box, Text } from "../";

<Meta title="Components/Grid" component={Grid} />

# Grid

Grid creates a Grid container that exposes CSS Grid properties

<Preview withToolbar>
  <Story name="grid">
    <Grid gridTemplateColumns="1fr 2fr" gridColumnGap="lg">
      <Box bg="text" p="md" mb="lg">
        <Text variant="body" color="textInverse">
          This is a grid box
        </Text>
      </Box>
      <Box bg="text" p="md" mb="lg">
        <Text variant="body" color="textInverse">
          This is a grid box
        </Text>
      </Box>
    </Grid>
  </Story>
</Preview>

## Component Props

<Props of={Grid} />
