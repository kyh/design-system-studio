import React from "react";
import { Box, Heading, Text } from "@dss/proto";
import { themesActions } from "features/themes/themesSlice";

export const ComponentContent = ({ componentKey, dispatch }: any) => {
  return (
    <Box>
      <Box>
        <Heading>Typography</Heading>
      </Box>
      <Box></Box>
    </Box>
  );
};
