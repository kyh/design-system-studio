import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { Spinner } from "./spinner";
import { Flex } from "../flex";
import { Text } from "../text";

<Meta title="Components/Spinner" component={Spinner} />

# Spinner

Spinning SVG loading indicator

<Preview withToolbar>
  <Story name="default">
    <Spinner />
  </Story>
</Preview>

Control size using the `size` prop, which maps to `theme.fontSizes`

<Preview>
  <Story name="sizing">
    <Flex alignItems="center">
      <Spinner size="displayXLarge" mr={2} />
      <Spinner size="displayLarge" mr={2} />
      <Spinner size="displayMedium" mr={2} />
      <Spinner size="displaySmall" mr={2} />
    </Flex>
  </Story>
</Preview>

Control color using the `color` prop

<Preview>
  <Story name="color">
    <Spinner color="primary.base" p={4} block={false} size="displaySmall" />
  </Story>
</Preview>

### Inline

<Preview withSource="open">
  <Story name="inline">
    <Text>
      this is an inline <Spinner block={false} /> spinner
    </Text>
  </Story>
</Preview>

### Block

<Preview withSource="open">
  <Story name="block">
    <Text>
      this is a block <Spinner block p={3} /> spinner (default)
    </Text>
  </Story>
</Preview>

## Component Props

<Props of={Spinner} />
