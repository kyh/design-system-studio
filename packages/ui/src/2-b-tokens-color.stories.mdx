import {
  Meta,
  Subtitle,
  ColorPalette,
  ColorItem,
} from "@storybook/addon-docs/blocks";
import { palette } from "./theme/shared-tokens";

<Meta title="Foundations/Design Tokens/Color" />

export const createPalette = (palette) => {
  delete palette.displayName;
  delete palette.__docgenInfo;
  return Object.keys(palette).map((colorKey) => ({
    title: colorKey,
    subtitle: `theme.palette.${colorKey}`,
    colors: Object.values(palette[colorKey]),
  }));
};

# Color Tokens

<Subtitle>
  Colors are designed to communicate actions, state and direction within the
  interface. We provide light and dark color modes. However, the shape of the
  colors object is same for each mode. This allows us to build components with a
  consistent API.
</Subtitle>

### Palette

<ColorPalette>
  {createPalette(palette).map((colorItem) => (
    <ColorItem key={colorItem.title} {...colorItem} />
  ))}
</ColorPalette>

Resources:

- Lyft Color Box - https://design.lyft.com/re-approaching-color-9e604ba22c88
